# План фазы 2: бэкенд поверх демо-фронтенда

**Статус:** 23.09.2026. Фаза 2 в коде. Управляющий документ фазы 3 — `NotMice_TZ_Faza3.md`.

**Управляющий документ фазы 2:** `docx/notmice_TZ_Faza2.md` (Андрей, 20.09.2026).

Этот файл заменяет прежний 28-дневный план от 18.09 (`TZ_MVP_28_DAYS.md` + `TECHNICAL_ARCHITECTURE.md` как недельная раскладка). Исходное ТЗ остаётся источником научных и правовых ограничений; календарь, стек фронта, non-goals и формулировка приватности берутся из ТЗ фазы 2.

Неподвижные даты из исходного ТЗ (ТЗ фазы 2 их не меняет):

| Веха | Дата |
| :--- | :--- |
| Гейт трека А | 05.10.2026 |
| Код-фриз | 10.10.2026 |
| Подача SPRIND | 16.10.2026, 18:00 CET |

---

## 0. Что изменилось относительно старого PLAN.md

Старый план описывал greenfield на 28 дней: сначала инфраструктура, потом Astro-сайт как отдельный этап 7. Фаза 1 уже потратила время на React-витрину. ТЗ фазы 2 это фиксирует и пересчитывает календарь от 20.09.

| Тема | Было в PLAN.md (18.09) | Утверждено по ТЗ фазы 2 |
| :--- | :--- | :--- |
| Объект работы | Новый продукт с нуля, включая контентный сайт | Backend под уже существующий React/Vite-демо |
| Фронтенд | Astro + Tailwind, этап 7 «сделать сайт» | React/Vite фазы 1 остаётся витриной. Переписывать на Astro в эти дни запрещено |
| Vision LLM | Только Anthropic Claude 3.5 Sonnet | Claude **или** Gemini, вызов только с backend, ключ в env сервера |
| Календарь | «Дни 1–28» без привязки к датам | Модули 20.09–10.10 |
| Приватность | «оригиналы не хранятся, метаданные хранятся» | Слоган Zero Cloud Retention снимается. PDF никогда на диск; подтверждённые значения — в Postgres с согласия |
| Хеш | Не уточнён | Настоящий SHA-256 в provenance, не integer-hash с `0x` |
| Auth | «seed-фразовый вход» без криптодеталей | Настоящий BIP-39; в БД только argon2id-хеш фразы |
| API | Общее «read-only OpenAPI» | Контракт из раздела 4 ТЗ фазы 2, включая `/healthz` и timeseries |
| Словарь | «подготовить LOINC-маппинг» | Версионированный YAML/JSON, ≥20 маркеров, RU/EN синонимы, очередь `unmapped`. Только LOINC, без SNOMED |
| Схема БД | User, UploadSession, LabResult, Biomarker, Provenance, ShareSetting | Минимум: `users`, `lab_results`, `biomarkers`, `provenance`, `share_settings`. `pgvector` закладывается, в MVP не используется |
| Non-goals | mobile, Telegram, raw PDF, токеномика, диагнозы | Плюс: IPFS, ZK-vault, Tesseract WASM, SNOMED, Astro-rewrite, wearables, платежи, лидерборд, API лабораторий, соцфункции |
| Лендинг | Отдельный этап сборки Astro-сайта | Текст лендинга правится (убрать IPFS/ZK/Zero Cloud Retention). Новый маркетинг-сайт не строится |

Открытые решения Андрея (раздел 8 ТЗ фазы 2) в этом плане **не закрываются**. Рабочая схема ниже — рекомендация ТЗ; пока нет явного «да», в заявке SPRIND нельзя задним числом менять стек.

---

## 1. Цель фазы 2

К код-фризу 10.10 должен работать не бутафорский, а реальный конвейер данных — чтобы 16.10 подавать заявку с живым пайплайном.

Сквозной сценарий (DoD):

1. Пользователь создаёт псевдонимный аккаунт и один раз получает BIP-39 seed-фразу.
2. Загружает PDF/скан анализов.
3. Сервер в RAM считает SHA-256, извлекает маркеры, файл уничтожается.
4. Человек проверяет и правит цифры, затем подтверждает.
5. Система считает PhenoAge (исследовательский индекс, не медуслуга).
6. При включённом тумблере публичности обезличенные строки видны в `/api/v1/dataset` и CC0-выгрузке.

Текущий фронт на `notmice.ai.studio` — витрина на фикстурах из `src/data/phenoAgeData.ts`. Его не выкидываем: подключаем к backend.

---

## 2. Архитектура

```text
React/Vite SPA (фаза 1, уже есть)
        HTTPS
FastAPI (Python 3.12, async)
        SQLAlchemy 2.0 + Alembic
        PostgreSQL 16
        Claude Vision или Gemini — только серверно, ключ в env
```

Чистая архитектура backend (из `TECHNICAL_ARCHITECTURE.md`, без изменений):

```text
app/
├── core/          # config.py (pydantic-settings), logging.py, deps.py
├── domain/        # Pydantic v2, Enum, PII-валидаторы
├── repositories/  # SQLAlchemy async, без бизнес-логики
├── services/      # парсинг, LOINC, PhenoAge, экспорт
├── api/           # FastAPI-роутеры: валидация входа + вызов сервисов
├── migrations/    # Alembic
└── tests/         # pytest
```

Правило: роутер не знает про БД; репозиторий не знает про HTTP; сервис не знает ни про то, ни про другое.

Качество: ruff (zero warnings), mypy `--strict`, pytest, structlog, Google-style docstrings, секреты только через `.env` + pydantic-settings. Лицензии: код AGPL-3.0, словари CC-BY-4.0, экспорт датасета CC0.

Docker Compose: `api` / `postgres` / `proxy`.

### 2.1. Приватность (формулировка для кода, сайта и заявки)

| Что | Статус |
| :--- | :--- |
| Оригинал файла (PDF/фото) | Никогда не пишется на диск, только RAM, уничтожается сразу после подтверждения |
| SHA-256 хеш оригинала | Хранится в provenance — настоящий SHA-256 |
| Подтверждённые значения биомаркеров | В Postgres с согласия, привязка к псевдонимному ID, не к ФИО |
| Имя, дата рождения, номер пациента | Не собираются. PII-валидаторы отсекают на входе и на выходе |
| Публичный агрегат (opt-in) | `/api/v1/dataset` только для профилей с явно включённой публичностью |

Сайт и заявка говорят: *оригиналы документов не хранятся; структурированные значения хранятся с согласия; для исследовательского и коммерческого переиспользования обезличенных агрегатов.* Слоган «Zero Cloud Retention» с лендинга снимается.

---

## 3. API-контракт и схема БД

| Метод | Путь | Что делает |
| :--- | :--- | :--- |
| POST | `/api/v1/accounts` | Псевдонимный участник, BIP-39 фраза один раз; в БД только argon2id-хеш |
| POST | `/api/v1/uploads/extract` | PDF/скан в RAM → SHA-256 → pdfplumber или Vision LLM → JSON маркеров → файл уничтожается |
| POST | `/api/v1/uploads/confirm` | Подтверждённые значения + provenance в Postgres |
| GET | `/api/v1/dataset` | Read-only, rate-limit по IP, только opt-in строки, CSV/Parquet + datasheet |
| GET | `/api/v1/profiles/{id}/timeseries` | Временной ряд биомаркеров одного публичного профиля |
| GET | `/healthz` | Проверка БД, обязательна по DoD модуля 1 |

Таблицы минимум: `users`, `lab_results`, `biomarkers`, `provenance`, `share_settings`.

`UploadSession` из старого плана не входит в минимум ТЗ фазы 2 — не плодим таблицу без нужды; сессия извлечения живёт в памяти/во временном токене ответа extract, на диск не садится.

`pgvector` закладывается в схему под семантический поиск по публикациям (Phase 0), в MVP не используется.

---

## 4. Модули и календарь

Календарь ТЗ фазы 2 от 20.09. На 21.09 разработка ещё не стартовала: у модуля 1 уже сожжён 1 день буфера. Даты вех 05.10 / 10.10 / 16.10 не сдвигаем. Сжатие — только внутри модулей, не расширением scope.

### Модуль 1. Каркас и инфраструктура — 20–23.09

Задачи:

- Репозиторий backend, Docker Compose (`api` / `postgres` / `proxy`).
- Alembic, pydantic-settings, structlog.
- CI: ruff + mypy strict + pytest.
- AGPL-3.0 в репозитории.
- Модели и миграции: `users`, `lab_results`, `biomarkers`, `provenance`, `share_settings`.
- `GET /healthz`.

DoD: поднимается одним Compose, миграции проходят, `/healthz` зелёный, CI на PR.

### Модуль 2. Псевдонимные аккаунты — 23–26.09

Задачи:

- `POST /api/v1/accounts`: генерация BIP-39, показ фразы один раз.
- В БД только argon2id-хеш, без email/телефона.
- Вход по фразе вместо 12 слов-заглушки в UI.
- Тумблер публичности → `share_settings`.

DoD: создать аккаунт, выйти, войти той же фразой. Заглушка `INITIAL_MNEMONIC` в React больше не является источником истины.

### Модуль 3. Приём и парсинг — 26.09–01.10

Стартовать только после закрытия открытых решений раздела 8 (хотя бы Vision-модель).

Задачи:

- `POST /api/v1/uploads/extract`: MIME, размер, SHA-256 в RAM.
- Текстовый PDF → pdfplumber.
- Скан/фото → выбранный Vision LLM, structured output, tenacity retry.
- Ответ — JSON маркеров для UI review. Файл на диск не пишется.
- `setTimeout`-OCR и пресеты Quest в `UploadLabTab` заменяются вызовом API.

DoD: реальный PDF даёт извлечённые цифры, не пресет. После confirm исходник из памяти исчезает.

### Модуль 4. LOINC-словарь — 29.09–02.10 (параллельно с модулем 3)

Задачи:

- Вынести 9 уже верных кодов из `phenoAgeData.ts` в версионированный YAML/JSON backend, лицензия CC-BY-4.0.
- Расширить до ≥20 маркеров (9 PhenoAge + 11 частых).
- Синонимы RU/EN, единицы, коэффициенты конверсии, допустимый диапазон.
- Очередь `unmapped` — нераспознанное не теряется.

Стандарт фазы 2 — только LOINC. SNOMED не обещаем.

DoD: альбумин «Serum Albumin» / «Альбумин» → `1751-7`; неизвестное имя → `unmapped`, не drop.

### Модуль 5. PhenoAge — 02–04.10

Задачи:

- Чистая функция без UI-зависимостей, коэффициенты Levine 2018.
- Юнит-тесты по публикации.
- Disclaimer: исследовательский индекс, не медуслуга — в API и в UI.

DoD: расчёт совпадает с эталонными кейсами; контроллеры формулу не содержат.

### Модуль 6. Публичный Data API — 04–07.10

Задачи:

- OpenAPI 3.1, только чтение.
- Rate-limit по IP.
- `GET /api/v1/dataset` и `GET /api/v1/profiles/{id}/timeseries`.
- PII-валидаторы на выходе: нет ФИО, даты рождения, внутренних чувствительных ID.

DoD: opt-out профиль в выдаче не появляется; opt-in — появляется обезличенным.

### Модуль 7. Экспорт и datasheet — 07–09.10

Задачи:

- CSV и Parquet.
- Файл datasheet (Datasheets for Datasets).
- Лицензия выгрузки CC0.
- Без IPFS и без «Parquet-на-IPFS».

DoD: выгрузка собирается из Postgres, не из браузерного state.

### Буфер / сквозной сценарий — 09–10.10

Один человек, не разработчик, проходит путь: аккаунт → загрузка → проверка → PhenoAge → тумблер → строка в публичном API. Критические баги чинятся. Код-фриз 10.10.

---

## 5. Критический путь

1. Каркас + Postgres + `/healthz`
2. Аккаунты (BIP-39 / argon2id)
3. Extract + confirm (серверный парсинг)
4. LOINC + unmapped (параллельно с п. 3)
5. PhenoAge
6. Public API + export
7. Сквозной прогон и фриз

Контентный Astro-сайт из старого этапа 7 **снят с критического пути**. Фронт фазы 1 подключается к API по мере готовности модулей 2–3.

Правка текстов лендинга (убрать IPFS / ZK / Zero Cloud Retention / SNOMED) — не фича-этап, а срочный риск доверия. Делается отдельно, до показа Пешкину или жюри, по решению Андрея.

---

## 6. Осознанно не делаем в MVP

Обязательство не обещать это в заявке и не показывать как архитектуру:

Из исходного ТЗ:

- носимые устройства,
- платежи и подписка,
- публичный лидерборд,
- мобильное приложение,
- интеграции с API лабораторий,
- комментарии и соцфункции,
- Telegram-бот,
- хранение оригиналов документов,
- токеномика,
- врачебные назначения и диагнозы.

Новое, по ТЗ фазы 2:

- IPFS и Parquet-на-IPFS,
- «zero-knowledge vault» и крипто-терминология вокруг seed-фразы (это вход по кодовой фразе, не кошелёк),
- Tesseract WASM в браузере,
- SNOMED,
- переписывание фронта на Astro до 10.10.

---

## 7. Definition of Done к 10.10

MVP фазы 2 готов, если:

- пользователь регистрируется по настоящей BIP-39 seed-фразе;
- загружает PDF или изображение;
- сервер извлекает маркеры (не пресет Quest);
- человек подтверждает или правит значения;
- считается PhenoAge с дисклеймером;
- при opt-in данные видны в публичном API и CC0-выгрузке;
- оригинал не пишется на диск;
- provenance содержит настоящий SHA-256, лабораторию, дату забора, версию парсера, факт подтверждения;
- `/healthz` и CI зелёные;
- на лендинге нет утверждений про IPFS, ZK, WASM-OCR и Zero Cloud Retention, если они не реализованы.

---

## 8. Открытые решения — не стартовать модуль 3, пока не закрыты

| Вопрос | Кто | Статус на 21.09 | Рекомендация ТЗ фазы 2 |
| :--- | :--- | :--- | :--- |
| Astro или React/Vite | Андрей | Открыто | Оставить React/Vite |
| Claude Vision или Gemini | Андрей / Бактыбек | Открыто | Вопрос цены/качества, вызов только с backend |
| Звонок в лабораторию Кристины (гейт трека А, 05.10) | Кристина | Не в этом плане | Нужен ответ до 05.10 |
| Убрать IPFS/ZK/Zero Cloud Retention с лендинга до любых показов | Андрей | Срочно | Да, до показа Пешкину / жюри |

Без явного подтверждения двух первых пунктов исходный ТЗ формально в силе (Astro + Claude). Практический план фазы 2 всё равно строится вокруг React-витрины: иначе 20 дней уйдут на перепись фронта, и пайплайна к 10.10 не будет.

---

## 9. Риски

- **Буфер модуля 1 уже горит.** План писался 20.09, сверка 21.09, код не начат. Нельзя «доделать ещё чуть-чуть» на фронте вместо модуля 1 — этот соблазн уже сработал один раз.
- **Стек в заявке SPRIND.** Менять Astro→React или Claude→Gemini после подачи дороже, чем согласовать сейчас.
- **Лендинг врёт.** Показ витрины с IPFS/ZK/WASM как «архитектуры» — риск доверия, не косметика.
- **Vision без ключа и без решения.** Модуль 3 не начинать на самодельном OCR «на всякий случай».

Правила работы:

- Не хранить raw PDF/сканы.
- Не расширять scope за пределы разделов 4–6 ТЗ фазы 2.
- Список раздела 6 этого плана — обязательство, не wishlist.

---

## 10. Что будет после согласия на разработку

Первый коммит фазы 2 — только модуль 1: Compose, FastAPI, Postgres, Alembic, `/healthz`, CI. Фронт не переписывается. Фикстуры `phenoAgeData.ts` не удаляются, пока extract не подключён.

До этого согласия файлы приложения не меняются.
